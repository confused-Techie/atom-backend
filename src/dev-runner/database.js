/**
 * @module database (DevRunner)
 * @desc This DevRunner instance of Database is what is exported when the server 
 * detects that it is running in development mode, and requires ./src/database.js
 * Intended to safely mock all features of ./src/database.js without 
 * the risk of modifying remote data, or having to rely on external services.
 */
