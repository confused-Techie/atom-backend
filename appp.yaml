runtime: nodejs14

env_variables:
  PORT: 8080
  SEARCHALGORITHM: "lcs"
  SERVERURL: "http://localhost:8080"
  PAGINATE: 30
  CACHETIME: 600000
  DEBUGLOG: true
  FILESTORE: "filesystem"
  GCS_BUCKET: "bucket_name"
  GCS_SERVICE_ACCOUNT_FILE: "local_file_path"
